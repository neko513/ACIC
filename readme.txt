Paper "Adaptive Correlation Integration for Deep Image Clustering"
We run our code with TESLA V100-PCIE 32G in Python.
tensorflow   1.10.0
scipy 1.1.0
scikit-learn 0.20.0

Besides, we release our code with MNIST dataset as an example.

Run "python WIDC_batch_pair.py --datasource=FMNIST --batch=32 --lr=0.0008 --prior_num=15"
